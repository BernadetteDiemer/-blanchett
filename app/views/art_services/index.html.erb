<div class="art-services-index">
  <div class="general-container mt-3">
    <div class="row">
      <div class="col-12 index_title">
        <h1>🎈✨Our Art Services✨🎈</h1>
      </div>
    </div>

  <div class="row">
    <div class="col index_text">
      <% if @query %>
        <p>You searched for <strong><%= @query %></strong>. We found <%= @art_services.count %> art services out of <%= ArtService.count %> on our website.</p>
      <% else %>
        <p> Browse through our art services!
        <br> Available art services at the moment: <%= ArtService.count%></p>
      <% end %>
     <%# <%= link_to "Back to homepage", root_path, class: 'btn btn-primary' %>
    </div>
  </div>



<div class="index_grid">
  <% @art_services.each do |art_service| %>
   <%= link_to art_service_path(art_service), class: "art-service-link" do %>
      <div class="art_service-card">
        <% if art_service.photos.attached?%>
          <div class="art_service-card-image" style="background-image:linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path art_service.photos.first.key%>')"></div>
        <% else %>
          <div class="art_service-card-image" style="background-image:linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= image_path("default-art-service.jpg")%>')" ></div>

        <% end %>
        <div class="art_service-card-content">
          <div class="card-content-top">
            <h4><%= art_service.title %></h4>
            <div class="d-flex art_service_price">
              <i class="material-icons mr-2">€</i> <%= art_service.price %>
            </div>
          </div>
          <p><%= art_service.address %></p>

          <div></div>

        </div>

      </div>
    <% end %>
  <% end %>
</div>
